<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color when checked -->
    <item android:color="@color/checkedRadioColor" android:state_checked="true" />

    <!-- Default color when unchecked -->
    <item android:color="@color/uncheckedRadioColor" />
</selector>
